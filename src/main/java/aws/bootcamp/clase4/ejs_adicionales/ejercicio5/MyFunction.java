package aws.bootcamp.clase4.ejs_adicionales.ejercicio5;

/**
 * Interfaz funcional para una función personalizada que toma dos parámetros long.
 */
@FunctionalInterface
public interface MyFunction {
    int apply(int x, int y);
}
